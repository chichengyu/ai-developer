# Tkinter Threading Demo

Uses `scripts/threading_tkinter.py` directly -- no copy needed.

## Run

```powershell
python examples/tkinter-threading/app.py
```

## Package

```powershell
powershell -File scripts/build_python.ps1 `
  -Entry examples/tkinter-threading/app.py `
  -Name TkinterDemo
```
