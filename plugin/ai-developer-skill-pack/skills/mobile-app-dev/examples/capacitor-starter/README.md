# capacitor-starter

Minimal Capacitor app demonstrating the thin-shell pattern. The web
asset in `www/` is served by the native WebView.

```bash
npm install
npx cap add ios
npx cap add android
npx cap sync
npx cap open ios
npx cap open android
```

Build with `scripts/build_capacitor.ps1`.
