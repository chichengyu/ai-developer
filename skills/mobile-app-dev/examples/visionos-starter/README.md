# visionos-starter

Minimal visionOS SwiftUI starter. Create an Xcode visionOS app, then
add this `ImmersiveApp` file and replace the default window content.

Use `RealityView` for 3D content; keep 2D UI in `WindowGroup`.
