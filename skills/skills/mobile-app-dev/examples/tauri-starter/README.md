# tauri-starter

Minimal Tauri Mobile app demonstrating the Rust + WebView pattern.

```bash
npm install
npm run tauri android init
npm run tauri android build
```

Build with `scripts/build_tauri_mobile.ps1`.
